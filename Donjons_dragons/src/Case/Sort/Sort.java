package Case.Sort;
import Case.Items;

public class Sort extends Items {
    // Attributs
    // Constructeur
    public Sort (String pName, int pAttack) {
        super(pName, pAttack);
    }
    public Sort () {
        super("AGROU AGROU", 1);
    }
}
